{
  "name": "src",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "dif-dev-all": "cdk diff -c stage=dev --profile=dev",
    "dep-dev-all": "cdk deploy -c stage=dev --all --profile=dev --require-approval=never",
    "destroy-dev-all": "cdk destroy -c stage=dev -all --profile=dev"
  },
  "devDependencies": {
    "@swc/core": "^1.15.0",
    "@swc/jest": "^0.2.39",
    "@types/aws-lambda": "^8.10.162",
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1133.0",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.28.1",
    "jest": "^30",
    "prettier": "^3.9.6",
    "tsx": "^4.23.0",
    "typescript": "~7.0.2"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.262.0",
    "constructs": "^10.5.0"
  }
}
